// Code your solutions in this file
function writeCards(name, event) {
    let finishedCards = []
    for (let i = 0; i < name.length; i++) {
        finishedCards.push(`Thank you, ${name[i]}, for the wonderful ${event} gift!`)
    } return finishedCards
}






























function countDown(n) {
    let i = n;
    while (i >= 0)
    console.log(i--)
}